---
title: "AI Web Signals"
subtitle: "Observable AI Policy Signals Across the Tranco Top 100,000"
format:
  html:
    fig-width: 10
    fig-height: 6
---

```{r}
#| include: false
source("analysis/data.R")
list_id <- if (is.null(tranco_metadata$list_id)) "PYGVJ" else tranco_metadata$list_id
list_url <- if (is.null(tranco_metadata$list_url)) {
  "https://tranco-list.eu/list/PYGVJ"
} else {
  tranco_metadata$list_url
}
generated_on <- if (is.null(tranco_metadata$generated_on)) {
  "July 29, 2026"
} else {
  as.character(tranco_metadata$generated_on)
}
```

## Methodology

We scanned the top 100,000 pay-level domains from the standard Tranco list for
publicly observable AI-related signals in `robots.txt` and `llms.txt`.

The current inventory is the standard Tranco list
[`r list_id`](`r list_url`), generated on `r generated_on`. When
`data/input/tranco-metadata.json` is present, this report uses that file as the
source of truth for list IDs and exact-list URLs.

Tranco rank is an aggregate rank over pay-level domains. It should not be read
as traffic volume, exact visitors, page views, or HTTP reachability.

Analysis denominators should distinguish all 100,000 ranked domains selected,
domains for which an HTTP response was observed, domains with successfully
parsed `robots.txt`, and domains with a determined AI-policy result. Failed
network observations are not negative AI-policy observations.

## Citation

Victor Le Pochat, Tom Van Goethem, Samaneh Tajalizadehkhoob, Maciej
Korczyński, and Wouter Joosen. 2019. "Tranco: A Research-Oriented Top Sites
Ranking Hardened Against Manipulation." Proceedings of the 26th Annual Network
and Distributed System Security Symposium (NDSS 2019).
<https://doi.org/10.14722/ndss.2019.23386>
